import { h } from 'preact';
import Section from './ui/Section';

export default function Projects() {
  return (
    <Section className="text-xl" id="projects">
      <div>Nadji auto</div>
      <div>Stores</div>
      <div>Ads</div>
    </Section>
  );
}
